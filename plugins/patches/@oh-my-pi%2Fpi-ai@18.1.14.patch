diff --git a/dist/types/auth-broker/refresher.d.ts b/dist/types/auth-broker/refresher.d.ts
index a45daf7a413e3f9defcbc3fe17effe6602d92fdc..5e5676b1b5fa731c9c6366edb43d65d213910b13 100644
--- a/dist/types/auth-broker/refresher.d.ts
+++ b/dist/types/auth-broker/refresher.d.ts
@@ -18,7 +18,7 @@ export declare class AuthBrokerRefresher {
     #private;
     constructor(opts: AuthBrokerRefresherOptions);
     start(): void;
-    stop(): void;
+    stop(): Promise<void>;
     getSchedule(): AuthBrokerRefresherSchedule;
     /** Run one sweep. Exposed for tests. */
     tick(): Promise<void>;
diff --git a/dist/types/auth-broker/remote-store.d.ts b/dist/types/auth-broker/remote-store.d.ts
index bd2301f50b8b92616cddf23331910d629a01054e..00c768bf0e68872e551c496356798f21afb7b4fa 100644
--- a/dist/types/auth-broker/remote-store.d.ts
+++ b/dist/types/auth-broker/remote-store.d.ts
@@ -23,9 +23,9 @@ export interface RemoteAuthCredentialStoreOptions {
      */
     streamSnapshots?: boolean;
     /**
-     * Called with each broker-sourced raw full snapshot after the filtered
-     * public view is applied. The constructor's initial snapshot intentionally
-     * does not trigger this hook.
+     * Called with the accepted, filtered snapshot after a full snapshot or stream
+     * entry/removal update. Rejected frames do not trigger this hook. The
+     * constructor's initial snapshot intentionally does not trigger it.
      */
     onSnapshot?: (snapshot: SnapshotResponse, generation: number) => void;
     /**
@@ -62,11 +62,11 @@ export declare class RemoteAuthCredentialStore implements AuthCredentialStore {
     deleteCredentialBlocks(credentialId: number): void;
     cleanExpiredCredentialBlocks(nowMs: number): void;
     /**
-     * In-memory update from a successful refresh through the broker. AuthStorage
-     * calls this after `#replaceCredentialAt`; the broker already persisted the
-     * authoritative row, so we just mirror it.
+     * AuthStorage writes back its refresh result here, potentially after another
+     * snapshot has been accepted. Keep the broker snapshot authoritative instead
+     * of mirroring that caller-owned credential.
      */
-    updateAuthCredential(id: number, credential: AuthCredential): void;
+    updateAuthCredential(_id: number, _credential: AuthCredential): void;
     deleteAuthCredential(id: number, disabledCause: string): void;
     deleteAuthCredentialRemote(id: number, disabledCause: string): Promise<boolean>;
     tryDisableAuthCredentialIfMatches(id: number, _expectedData: string, disabledCause: string): boolean;
@@ -84,10 +84,8 @@ export declare class RemoteAuthCredentialStore implements AuthCredentialStore {
     deleteAuthCredentialsForProvider(_provider: string, _disabledCause: string): void;
     /**
      * Upsert a single credential through the broker. The broker server is the
-     * canonical writer — see `POST /v1/credential`. The redacted snapshot
-     * entries returned by the server replace the provider's rows in our local
-     * snapshot, and the global snapshot is then refreshed in the background so
-     * any concurrent peer (refresh, generation bump) stays in sync.
+     * canonical writer — see `POST /v1/credential`. Mutation replies may trail
+     * later writes, so only a fenced snapshot read can publish the current rows.
      */
     upsertAuthCredentialRemote(provider: string, credential: AuthCredential): Promise<StoredAuthCredential[]>;
     /**
@@ -98,8 +96,7 @@ export declare class RemoteAuthCredentialStore implements AuthCredentialStore {
     replaceAuthCredentialsRemote(provider: string, credentials: AuthCredential[]): Promise<StoredAuthCredential[]>;
     /**
      * Logout: disable every active credential for the provider on the broker,
-     * then drop them from the local snapshot. Refresh fetches the authoritative
-     * post-state in the background.
+     * then acquire the canonical post-state through the snapshot fence.
      */
     deleteAuthCredentialsRemote(provider: string, disabledCause: string): Promise<void>;
     getCache(key: string): string | null;
@@ -114,7 +111,7 @@ export declare class RemoteAuthCredentialStore implements AuthCredentialStore {
      * the broker-redacted credential with {@link REMOTE_REFRESH_SENTINEL} in
      * the `refresh` slot.
      */
-    refreshOAuthCredential(_provider: Provider, credentialId: number, _credential: OAuthCredential, signal?: AbortSignal): Promise<OAuthCredentials>;
+    refreshOAuthCredential(provider: Provider, credentialId: number, _credential: OAuthCredential, signal?: AbortSignal): Promise<OAuthCredentials>;
     /**
      * Store-level hook consumed by `AuthStorage.fetchUsageReports()` — proxies
      * to the broker's `/v1/usage` endpoint. The broker's egress IP isn't
diff --git a/dist/types/auth-broker/server.d.ts b/dist/types/auth-broker/server.d.ts
index b12f98cb320aa5cf1eb61429001db8d1de2d7620..bc591b04ad906f8049fabad71df5303005a51165 100644
--- a/dist/types/auth-broker/server.d.ts
+++ b/dist/types/auth-broker/server.d.ts
@@ -15,8 +15,12 @@ export interface AuthBrokerServerOptions {
     storage: AuthStorage;
     /** Listen address; accepts `host:port` or just `port`. */
     bind?: string;
-    /** Accept any of these bearer tokens. Empty disables auth (loopback only). */
+    /** Accept any of these bearer tokens. Empty disables auth only when hashes are omitted (loopback only). */
     bearerTokens: string[];
+    /** Additional SHA-256 bearer verifiers: a nonempty array of exact lowercase 64-character hex digests. */
+    bearerTokenHashes?: readonly string[];
+    /** Enable cooperative control endpoints for this configured plaintext bearer only. */
+    controlBearerToken?: string;
     /** Broker version string surfaced on `/v1/healthz`. */
     version?: string;
     /** Refresh credentials expiring within this window. Default 5 min. */
@@ -42,6 +46,8 @@ export interface AuthBrokerServerHandle {
     url: string;
     port: number;
     hostname: string;
+    /** Permanently stop work admission and drain storage, retaining the control listener. */
+    quiesce(): Promise<void>;
     close(): Promise<void>;
 }
 /** Boot the broker. Caller owns lifecycle; `handle.close()` to stop. */
diff --git a/dist/types/auth-storage.d.ts b/dist/types/auth-storage.d.ts
index 92b7d5082865dfa2f79fb6b964294e1fcb7b9e65..5b89f0a4ce88f63aba3b6cd5c0a0e7986a825f04 100644
--- a/dist/types/auth-storage.d.ts
+++ b/dist/types/auth-storage.d.ts
@@ -738,6 +738,8 @@ export declare class AuthStorage {
      * After calling this, the instance must not be reused.
      */
     close(): void;
+    /** Join owned usage/refresh work after the caller has stopped request/sweep admission. */
+    drainRefreshes(): Promise<void>;
     getGeneration(): number;
     /**
      * Reload state after another process commits to the backing store, then
diff --git a/src/auth-broker/refresher.ts b/src/auth-broker/refresher.ts
index 5bb4c0e25ef97948fdefcb51ac8942a0d3fa40a4..4bc57b1ae02fd25c39141072f2f89e9fa90c0926 100644
--- a/src/auth-broker/refresher.ts
+++ b/src/auth-broker/refresher.ts
@@ -37,7 +37,8 @@ export class AuthBrokerRefresher {
 	readonly #refreshIntervalMs: number;
 	readonly #now: () => number;
 	#timer: NodeJS.Timeout | undefined;
-	#running = false;
+	#inFlight: Promise<void> | undefined;
+	#stopped = false;
 	#nextSweepAt: number;
 	constructor(opts: AuthBrokerRefresherOptions) {
 		this.#storage = opts.storage;
@@ -49,6 +50,7 @@ export class AuthBrokerRefresher {
 
 	start(): void {
 		if (this.#timer !== undefined) return;
+		this.#stopped = false;
 		// Refresh sweep is best-effort; kick once immediately so freshly-booted
 		// brokers don't hand out near-expired tokens for the first interval.
 		this.#nextSweepAt = this.#now();
@@ -58,11 +60,13 @@ export class AuthBrokerRefresher {
 		}, this.#refreshIntervalMs);
 	}
 
-	stop(): void {
+	async stop(): Promise<void> {
+		this.#stopped = true;
 		if (this.#timer !== undefined) {
 			clearInterval(this.#timer);
 			this.#timer = undefined;
 		}
+		await this.#inFlight;
 	}
 
 	getSchedule(): AuthBrokerRefresherSchedule {
@@ -75,28 +79,32 @@ export class AuthBrokerRefresher {
 	}
 
 	/** Run one sweep. Exposed for tests. */
-	async tick(): Promise<void> {
-		if (this.#running) return;
-		this.#running = true;
-		this.#nextSweepAt = this.#now();
-		try {
-			await this.#storage.reload();
-			const snapshot = this.#storage.exportSnapshot();
-			const now = this.#now();
-			const deadline = now + this.#refreshSkewMs;
-			const targets: number[] = [];
-			for (const entry of snapshot.credentials) {
-				if (entry.credential.type !== "oauth") continue;
-				const expires = entry.credential.expires;
-				if (typeof expires !== "number" || !Number.isFinite(expires)) continue;
-				if (expires > deadline) continue;
-				targets.push(entry.id);
-			}
-			await Promise.all(targets.map(id => this.#refreshOne(id)));
-		} finally {
-			this.#running = false;
+	tick(): Promise<void> {
+		if (this.#stopped) return this.#inFlight ?? Promise.resolve();
+		if (this.#inFlight) return this.#inFlight;
+		this.#inFlight = this.#sweep().finally(() => {
+			this.#inFlight = undefined;
 			this.#nextSweepAt = this.#now() + this.#refreshIntervalMs;
+		});
+		return this.#inFlight;
+	}
+
+	async #sweep(): Promise<void> {
+		this.#nextSweepAt = this.#now();
+		await this.#storage.reload();
+		if (this.#stopped) return;
+		const snapshot = this.#storage.exportSnapshot();
+		const now = this.#now();
+		const deadline = now + this.#refreshSkewMs;
+		const targets: number[] = [];
+		for (const entry of snapshot.credentials) {
+			if (entry.credential.type !== "oauth") continue;
+			const expires = entry.credential.expires;
+			if (typeof expires !== "number" || !Number.isFinite(expires)) continue;
+			if (expires > deadline) continue;
+			targets.push(entry.id);
 		}
+		await Promise.all(targets.map(id => this.#refreshOne(id)));
 	}
 
 	async #refreshOne(id: number): Promise<void> {
diff --git a/src/auth-broker/remote-store.ts b/src/auth-broker/remote-store.ts
index 2efb284aa720c028e656eccd53d774d1526d6e35..4c8eda2865b54176e7454e1e1c5fc3ac3a405d96 100644
--- a/src/auth-broker/remote-store.ts
+++ b/src/auth-broker/remote-store.ts
@@ -11,7 +11,6 @@ import * as os from "node:os";
 import { getAppName, getInstallId, logger } from "@oh-my-pi/pi-utils";
 import {
 	type AuthCredential,
-	type AuthCredentialSnapshotEntry,
 	type AuthCredentialStore,
 	type DisabledCredentialSummary,
 	type OAuthCredential,
@@ -121,15 +120,6 @@ function snapshotBlocksChanged(previous: readonly SnapshotEntry[], next: readonl
 	return false;
 }
 
-function credentialEntryWithBlocks(
-	entry: AuthCredentialSnapshotEntry,
-	blocks: readonly CredentialBlockSnapshot[] | undefined,
-): SnapshotEntry {
-	const incoming: SnapshotEntry = { ...entry, rotatesInMs: null };
-	if (blocks && blocks.length > 0) incoming.blocks = [...blocks].sort(compareCredentialBlockSnapshots);
-	return incoming;
-}
-
 function emptySnapshot(): SnapshotResponse {
 	return {
 		generation: 0,
@@ -225,9 +215,9 @@ export interface RemoteAuthCredentialStoreOptions {
 	 */
 	streamSnapshots?: boolean;
 	/**
-	 * Called with each broker-sourced raw full snapshot after the filtered
-	 * public view is applied. The constructor's initial snapshot intentionally
-	 * does not trigger this hook.
+	 * Called with the accepted, filtered snapshot after a full snapshot or stream
+	 * entry/removal update. Rejected frames do not trigger this hook. The
+	 * constructor's initial snapshot intentionally does not trigger it.
 	 */
 	onSnapshot?: (snapshot: SnapshotResponse, generation: number) => void;
 	/**
@@ -257,6 +247,11 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 	#generation = 0;
 	#usageOverlays: Map<string, UsageReport> = new Map();
 	#backgroundAbort = new AbortController();
+	/** Only the current acquisition may publish snapshots, regardless of broker generation. */
+	#snapshotAuthority = 0;
+	#activeBackgroundAbort: AbortController | undefined;
+	#foregroundSnapshotQueue: Promise<void> = Promise.resolve();
+	#foregroundSnapshotCount = 0;
 	readonly #backgroundIdleMs: number;
 	/** Last foreground store use; background sync parks `#backgroundIdleMs` after this. */
 	#lastActivityMs = Date.now();
@@ -325,10 +320,14 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 		this.#snapshot = { ...snapshot, credentials };
 		this.#generation = generation;
 		this.#snapshotReceivedAt = nowMs;
+		this.#notifySnapshot();
+	}
+
+	#notifySnapshot(): void {
 		const onSnapshot = this.#onSnapshot;
 		if (!onSnapshot) return;
 		try {
-			onSnapshot(snapshot, generation);
+			onSnapshot(this.#snapshot, this.#generation);
 		} catch (error) {
 			logger.debug("auth-broker snapshot callback failed", { error: String(error) });
 		}
@@ -375,20 +374,26 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 	async #runBackground(): Promise<void> {
 		let backoffMs = BACKGROUND_BACKOFF_INITIAL_MS;
 		while (!this.#closed && !this.#backgroundAbort.signal.aborted) {
+			if (this.#foregroundSnapshotCount > 0) {
+				await this.#foregroundSnapshotQueue;
+				continue;
+			}
 			if (this.#idleRemainingMs() <= 0) {
 				this.#activityWakeup ??= Promise.withResolvers<void>();
 				await this.#activityWakeup.promise;
 				continue;
 			}
+			const authority = ++this.#snapshotAuthority;
 			const watchdog = this.#startIdleWatchdog();
+			this.#activeBackgroundAbort = watchdog.controller;
 			try {
 				if (this.#streamSnapshots && !this.#streamingUnsupported) {
 					try {
-						await this.#consumeSnapshotStream(watchdog.signal);
+						await this.#raceWithSignal(this.#consumeSnapshotStream(watchdog.signal, authority), watchdog.signal);
 						backoffMs = BACKGROUND_BACKOFF_INITIAL_MS;
 					} catch (error) {
 						if (this.#closed || this.#backgroundAbort.signal.aborted) break;
-						if (watchdog.idled()) continue;
+						if (authority !== this.#snapshotAuthority || watchdog.signal.aborted) continue;
 						if (error instanceof AuthBrokerStreamUnsupportedError) {
 							this.#streamingUnsupported = true;
 							logger.debug("auth-broker snapshot stream unsupported; falling back to long-poll");
@@ -401,22 +406,28 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 					continue;
 				}
 				try {
-					const result = await this.#client.fetchSnapshot({
-						ifGenerationGt: this.#generation,
-						waitMs: BACKGROUND_WAIT_MS,
-						signal: watchdog.signal,
-					});
+					const result = await this.#raceWithSignal(
+						this.#client.fetchSnapshot({
+							ifGenerationGt: this.#generation,
+							waitMs: BACKGROUND_WAIT_MS,
+							signal: watchdog.signal,
+						}),
+						watchdog.signal,
+					);
+					if (authority !== this.#snapshotAuthority || watchdog.signal.aborted || this.#closed) continue;
 					if (result.status === 200) this.#applySnapshot(result.snapshot, result.generation);
 					backoffMs = BACKGROUND_BACKOFF_INITIAL_MS;
 				} catch (error) {
 					if (this.#closed || this.#backgroundAbort.signal.aborted) break;
-					if (watchdog.idled()) continue;
+					if (authority !== this.#snapshotAuthority || watchdog.signal.aborted) continue;
 					logger.debug("auth-broker background snapshot sync failed", { error: String(error) });
 					await this.#backoffWait(backoffMs);
 					backoffMs = Math.min(BACKGROUND_BACKOFF_MAX_MS, backoffMs * 2);
 				}
 			} finally {
 				watchdog.stop();
+				watchdog.controller.abort();
+				if (this.#activeBackgroundAbort === watchdog.controller) this.#activeBackgroundAbort = undefined;
 			}
 		}
 	}
@@ -440,9 +451,8 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 	 * while something else keeps the event loop alive — typically our own
 	 * in-flight broker request, which is exactly what it exists to end.
 	 */
-	#startIdleWatchdog(): { signal: AbortSignal; idled: () => boolean; stop: () => void } {
+	#startIdleWatchdog(): { signal: AbortSignal; controller: AbortController; stop: () => void } {
 		const controller = new AbortController();
-		let idled = false;
 		let timer: Timer | undefined;
 		const arm = (): void => {
 			const remainingMs = this.#idleRemainingMs();
@@ -451,13 +461,12 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 				timer.unref?.();
 				return;
 			}
-			idled = true;
 			controller.abort(new AIError.AbortError("auth-broker background sync idle"));
 		};
 		arm();
 		return {
 			signal: AbortSignal.any([this.#backgroundAbort.signal, controller.signal]),
-			idled: () => idled,
+			controller,
 			stop: () => clearTimeout(timer),
 		};
 	}
@@ -483,25 +492,30 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 		}
 	}
 
-	async #consumeSnapshotStream(signal: AbortSignal): Promise<void> {
+	async #consumeSnapshotStream(signal: AbortSignal, authority: number): Promise<void> {
 		const iterator = this.#client.openSnapshotStream({ signal });
+		let initialEvent = true;
 		try {
 			for await (const event of iterator) {
-				if (this.#closed || signal.aborted) break;
+				if (this.#closed || signal.aborted || authority !== this.#snapshotAuthority) break;
 				this.#streamingActive = true;
-				this.#applyStreamEvent(event);
+				this.#applyStreamEvent(event, initialEvent);
+				initialEvent = false;
 			}
 		} finally {
-			this.#streamingActive = false;
+			if (authority === this.#snapshotAuthority) this.#streamingActive = false;
 		}
 	}
 
-	#applyStreamEvent(event: SnapshotStreamEvent): void {
+	#applyStreamEvent(event: SnapshotStreamEvent, initialEvent: boolean): void {
 		switch (event.kind) {
 			case "snapshot": {
 				// Strip the discriminator so we store the wire-shape SnapshotResponse.
 				const { kind: _kind, ...snapshot } = event;
-				if (snapshot.generation < this.#generation) {
+				// Generations are process-local. Each new stream starts with an
+				// authoritative full snapshot, even after a broker restart resets it.
+				// Later frames still cannot roll back this stream's accepted state.
+				if (!initialEvent && snapshot.generation < this.#generation) {
 					logger.debug("auth-broker stream snapshot older than local; ignoring", {
 						local: this.#generation,
 						incoming: snapshot.generation,
@@ -548,6 +562,7 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 		this.#snapshot = { ...this.#snapshot, generation, serverNowMs, refresher, credentials };
 		this.#generation = generation;
 		this.#snapshotReceivedAt = Date.now();
+		this.#notifySnapshot();
 	}
 
 	#removeStreamCredential(
@@ -564,16 +579,73 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 		this.#snapshot = { ...this.#snapshot, generation, serverNowMs, refresher, credentials };
 		this.#generation = generation;
 		this.#snapshotReceivedAt = Date.now();
+		this.#notifySnapshot();
 	}
 
 	/** Re-hydrate the in-memory snapshot from the broker. */
 	async refreshSnapshot(): Promise<SnapshotResponse> {
-		this.#noteActivity();
-		const result = await this.#client.fetchSnapshot();
-		if (result.status === 200) this.#applySnapshot(result.snapshot, result.generation);
+		await this.#acquireSnapshot();
 		return this.#snapshot;
 	}
 
+	/**
+	 * Foreground reads take authority before opening a request. Retiring a stream
+	 * must not wait for its iterator to honor abort; its late frames are fenced.
+	 * Queue direct reads so a delayed response cannot overwrite a newer one.
+	 */
+	async #acquireSnapshot(maxWaitMs?: number, callerSignal?: AbortSignal): Promise<boolean> {
+		const signal = callerSignal
+			? AbortSignal.any([this.#backgroundAbort.signal, callerSignal])
+			: this.#backgroundAbort.signal;
+		signal.throwIfAborted();
+		this.#noteActivity();
+		const previousGeneration = this.#generation;
+		const deadline = maxWaitMs === undefined ? undefined : performance.now() + maxWaitMs;
+		let queueTimer: ReturnType<typeof setTimeout> | undefined;
+		let queueExpired = false;
+		const queuedTimeout = deadline === undefined ? undefined : new Promise<boolean>(resolve => {
+			queueTimer = setTimeout(() => {
+				queueExpired = true;
+				resolve(false);
+			}, maxWaitMs);
+		});
+		if (this.#foregroundSnapshotCount++ === 0) {
+			++this.#snapshotAuthority;
+			this.#streamingActive = false;
+			this.#activeBackgroundAbort?.abort();
+		}
+		const result = this.#foregroundSnapshotQueue.then(async () => {
+			if (queueTimer !== undefined) clearTimeout(queueTimer);
+			signal.throwIfAborted();
+			if (queueExpired) return false;
+			if (maxWaitMs !== undefined && this.#generation !== previousGeneration) return true;
+			const authority = ++this.#snapshotAuthority;
+			const response = await this.#raceWithSignal(
+				this.#client.fetchSnapshot({
+					...(deadline === undefined ? {} : {
+						ifGenerationGt: this.#generation,
+						waitMs: Math.max(0, Math.ceil(deadline - performance.now())),
+					}),
+					signal,
+				}),
+				signal,
+			);
+			signal.throwIfAborted();
+			if (authority !== this.#snapshotAuthority || this.#closed) return false;
+			if (response.status === 200) this.#applySnapshot(response.snapshot, response.generation);
+			return this.#generation !== previousGeneration;
+		});
+		this.#foregroundSnapshotQueue = result.then(
+			() => { this.#foregroundSnapshotCount--; },
+			() => { this.#foregroundSnapshotCount--; },
+		);
+		try {
+			return await this.#raceWithSignal(queuedTimeout ? Promise.race([result, queuedTimeout]) : result, signal);
+		} finally {
+			if (queueTimer !== undefined) clearTimeout(queueTimer);
+		}
+	}
+
 	listAuthCredentials(provider?: string): StoredAuthCredential[] {
 		this.#noteActivity();
 		const out: StoredAuthCredential[] = [];
@@ -694,17 +766,13 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 	}
 
 	/**
-	 * In-memory update from a successful refresh through the broker. AuthStorage
-	 * calls this after `#replaceCredentialAt`; the broker already persisted the
-	 * authoritative row, so we just mirror it.
+	 * AuthStorage writes back its refresh result here, potentially after another
+	 * snapshot has been accepted. Keep the broker snapshot authoritative instead
+	 * of mirroring that caller-owned credential.
 	 */
-	updateAuthCredential(id: number, credential: AuthCredential): void {
+	updateAuthCredential(_id: number, _credential: AuthCredential): void {
 		this.#noteActivity();
-		for (const entry of this.#snapshot.credentials) {
-			if (entry.id !== id) continue;
-			entry.credential = credential as typeof entry.credential;
-			return;
-		}
+		this.#maybeRefreshSnapshot("credential update");
 	}
 
 	deleteAuthCredential(id: number, disabledCause: string): void {
@@ -721,8 +789,7 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 		const found = this.#snapshot.credentials.some(entry => entry.id === id);
 		if (!found) return false;
 		await this.#client.disableCredential(id, disabledCause);
-		this.#removeCredentialById(id);
-		this.#maybeRefreshSnapshot("delete credential");
+		await this.#acquireSnapshot();
 		return true;
 	}
 
@@ -735,15 +802,7 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 	}
 
 	async waitForFreshSnapshot(maxWaitMs: number, opts: { signal?: AbortSignal } = {}): Promise<boolean> {
-		this.#noteActivity();
-		const previousGeneration = this.#generation;
-		const result = await this.#client.fetchSnapshot({
-			ifGenerationGt: this.#generation,
-			waitMs: maxWaitMs,
-			signal: opts.signal,
-		});
-		if (result.status === 200) this.#applySnapshot(result.snapshot, result.generation);
-		return this.#generation !== previousGeneration;
+		return this.#acquireSnapshot(maxWaitMs, opts.signal);
 	}
 
 	async prepareForRequest(credentialId: number, opts: { signal?: AbortSignal } = {}): Promise<boolean> {
@@ -757,16 +816,13 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 
 	async markCredentialSuspect(credentialId: number, opts: { signal?: AbortSignal } = {}): Promise<void> {
 		this.#noteActivity();
-		const { entry } = await this.#client.refreshCredential(credentialId, opts.signal);
-		if (entry.credential.type !== "oauth") {
-			throw new AIError.AuthBrokerError(`Broker returned non-OAuth credential for id=${credentialId}`);
-		}
-		if (!this.#applyCredentialEntry(entry)) {
-			throw new AIError.AuthBrokerError(
-				`Broker refreshed credential id=${credentialId} outside the configured account pool`,
-			);
+		const provider = this.#snapshot.credentials.find(entry => entry.id === credentialId)?.provider;
+		await this.#client.refreshCredential(credentialId, opts.signal);
+		await this.#acquireSnapshot(undefined, opts.signal);
+		const entry = this.#snapshot.credentials.find(candidate => candidate.id === credentialId);
+		if (!entry || entry.provider !== provider || entry.credential.type !== "oauth") {
+			throw new AIError.AuthBrokerError(`Broker refreshed credential id=${credentialId} is no longer available`);
 		}
-		this.#maybeRefreshSnapshot("suspect credential refresh");
 	}
 
 	replaceAuthCredentialsForProvider(_provider: string, _credentials: AuthCredential[]): StoredAuthCredential[] {
@@ -789,16 +845,13 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 
 	/**
 	 * Upsert a single credential through the broker. The broker server is the
-	 * canonical writer — see `POST /v1/credential`. The redacted snapshot
-	 * entries returned by the server replace the provider's rows in our local
-	 * snapshot, and the global snapshot is then refreshed in the background so
-	 * any concurrent peer (refresh, generation bump) stays in sync.
+	 * canonical writer — see `POST /v1/credential`. Mutation replies may trail
+	 * later writes, so only a fenced snapshot read can publish the current rows.
 	 */
 	async upsertAuthCredentialRemote(provider: string, credential: AuthCredential): Promise<StoredAuthCredential[]> {
 		this.#noteActivity();
-		const { entries } = await this.#client.uploadCredential(provider, credential);
-		this.#applyProviderEntries(provider, entries);
-		this.#maybeRefreshSnapshot("upload");
+		await this.#client.uploadCredential(provider, credential);
+		await this.#acquireSnapshot();
 		return this.listAuthCredentials(provider);
 	}
 
@@ -823,21 +876,16 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 				});
 			}
 		}
-		// Snapshot reflects the disables before we add the new rows so a concurrent
-		// reader cannot momentarily see old + new together for the same provider.
-		this.#removeProviderEntries(provider);
 		for (const credential of credentials) {
-			const { entries } = await this.#client.uploadCredential(provider, credential);
-			this.#applyProviderEntries(provider, entries);
+			await this.#client.uploadCredential(provider, credential);
 		}
-		this.#maybeRefreshSnapshot("replace");
+		await this.#acquireSnapshot();
 		return this.listAuthCredentials(provider);
 	}
 
 	/**
 	 * Logout: disable every active credential for the provider on the broker,
-	 * then drop them from the local snapshot. Refresh fetches the authoritative
-	 * post-state in the background.
+	 * then acquire the canonical post-state through the snapshot fence.
 	 */
 	async deleteAuthCredentialsRemote(provider: string, disabledCause: string): Promise<void> {
 		const existing = this.listAuthCredentials(provider);
@@ -852,46 +900,7 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 				});
 			}
 		}
-		this.#removeProviderEntries(provider);
-		this.#maybeRefreshSnapshot("delete");
-	}
-
-	#applyProviderEntries(provider: string, entries: AuthCredentialSnapshotEntry[]): void {
-		// `entries` is the broker's authoritative post-upsert list of rows for
-		// `provider`. Drop our existing rows for the same provider and splice in
-		// the fresh set — preserving every other provider's rows in place.
-		const existingBlocks = new Map(
-			this.#snapshot.credentials
-				.filter(entry => entry.provider === provider && entry.blocks !== undefined)
-				.map(entry => [entry.id, entry.blocks] as const),
-		);
-		const others = this.#snapshot.credentials.filter(entry => entry.provider !== provider);
-		const incoming = entries
-			.filter(entry => isCredentialInAccountPool(entry, this.#accountPool))
-			.map(entry => credentialEntryWithBlocks(entry, existingBlocks.get(entry.id)));
-		this.#snapshot = { ...this.#snapshot, credentials: [...others, ...incoming] };
-	}
-	#applyCredentialEntry(entry: AuthCredentialSnapshotEntry): boolean {
-		if (!isCredentialInAccountPool(entry, this.#accountPool)) {
-			this.#removeCredentialById(entry.id);
-			return false;
-		}
-		const index = this.#snapshot.credentials.findIndex(candidate => candidate.id === entry.id);
-		const existingBlocks = index === -1 ? undefined : this.#snapshot.credentials[index]?.blocks;
-		const incoming = credentialEntryWithBlocks(entry, existingBlocks);
-		if (index === -1) {
-			this.#snapshot = { ...this.#snapshot, credentials: [...this.#snapshot.credentials, incoming] };
-			return true;
-		}
-		const credentials = [...this.#snapshot.credentials];
-		credentials[index] = incoming;
-		this.#snapshot = { ...this.#snapshot, credentials };
-		return true;
-	}
-
-	#removeProviderEntries(provider: string): void {
-		const next = this.#snapshot.credentials.filter(entry => entry.provider !== provider);
-		this.#snapshot = { ...this.#snapshot, credentials: next };
+		await this.#acquireSnapshot();
 	}
 
 	#removeCredentialById(id: number): void {
@@ -1030,25 +1039,17 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 	 * the `refresh` slot.
 	 */
 	async refreshOAuthCredential(
-		_provider: Provider,
+		provider: Provider,
 		credentialId: number,
 		_credential: OAuthCredential,
 		signal?: AbortSignal,
 	): Promise<OAuthCredentials> {
 		this.#noteActivity();
-		const { entry } = await this.#client.refreshCredential(credentialId, signal);
-		if (entry.credential.type !== "oauth") {
-			throw new AIError.AuthBrokerError(`Broker returned non-OAuth credential for id=${credentialId}`);
-		}
-		if (!this.#applyCredentialEntry(entry)) {
-			throw new AIError.AuthBrokerError(
-				`Broker refreshed credential id=${credentialId} outside the configured account pool`,
-			);
-		}
-		if (!this.#streamingActive) {
-			await this.refreshSnapshot().catch(error => {
-				logger.debug("auth-broker snapshot refresh after credential refresh failed", { error: String(error) });
-			});
+		await this.#client.refreshCredential(credentialId, signal);
+		await this.#acquireSnapshot(undefined, signal);
+		const entry = this.#snapshot.credentials.find(candidate => candidate.id === credentialId);
+		if (!entry || entry.provider !== provider || entry.credential.type !== "oauth") {
+			throw new AIError.AuthBrokerError(`Broker refreshed credential id=${credentialId} is no longer available`);
 		}
 		const refreshed = entry.credential;
 		return {
@@ -1173,13 +1174,15 @@ export class RemoteAuthCredentialStore implements AuthCredentialStore {
 
 	/**
 	 * Reject the awaited promise when the caller's signal aborts, without
-	 * affecting the shared upstream fetch. Used to give each caller their
-	 * own cancel without one caller's abort cascading into a peer's in-flight
-	 * request through the single-flight `#usageInflight`.
+	 * affecting shared upstream work. Also releases retired snapshot acquisitions
+	 * even if their transport or iterator ignores abort.
 	 */
 	#raceWithSignal<T>(promise: Promise<T>, signal?: AbortSignal): Promise<T> {
 		if (!signal) return promise;
-		if (signal.aborted) return Promise.reject(new AIError.AbortError("auth-broker request aborted"));
+		if (signal.aborted) {
+			void promise.catch(() => {});
+			return Promise.reject(new AIError.AbortError("auth-broker request aborted"));
+		}
 		return new Promise<T>((resolve, reject) => {
 			const onAbort = (): void => {
 				signal.removeEventListener("abort", onAbort);
diff --git a/src/auth-broker/server.ts b/src/auth-broker/server.ts
index 48bd0c9a269df9aa0db6f347c036bc97e5216df3..981da451bfea2f1b1aa48a670e09dd030e98d88f 100644
--- a/src/auth-broker/server.ts
+++ b/src/auth-broker/server.ts
@@ -10,9 +10,11 @@
  * the server only checks a bearer token against an allow-list per request.
  */
 
+import { createHash, timingSafeEqual } from "node:crypto";
 import { type Type, type } from "@oh-my-pi/omptype";
 import { logger } from "@oh-my-pi/pi-utils";
 import type { AuthStorage, StoredCredentialBlock } from "../auth-storage";
+import * as AIError from "../error";
 import { parseBind } from "../utils/parse-bind";
 import { AuthBrokerRefresher, type AuthBrokerRefresherSchedule } from "./refresher";
 import type {
@@ -55,8 +57,12 @@ export interface AuthBrokerServerOptions {
 	storage: AuthStorage;
 	/** Listen address; accepts `host:port` or just `port`. */
 	bind?: string;
-	/** Accept any of these bearer tokens. Empty disables auth (loopback only). */
+	/** Accept any of these bearer tokens. Empty disables auth only when hashes are omitted (loopback only). */
 	bearerTokens: string[];
+	/** Additional SHA-256 bearer verifiers: a nonempty array of exact lowercase 64-character hex digests. */
+	bearerTokenHashes?: readonly string[];
+	/** Enable cooperative control endpoints for this configured plaintext bearer only. */
+	controlBearerToken?: string;
 	/** Broker version string surfaced on `/v1/healthz`. */
 	version?: string;
 	/** Refresh credentials expiring within this window. Default 5 min. */
@@ -83,6 +89,8 @@ export interface AuthBrokerServerHandle {
 	url: string;
 	port: number;
 	hostname: string;
+	/** Permanently stop work admission and drain storage, retaining the control listener. */
+	quiesce(): Promise<void>;
 	close(): Promise<void>;
 }
 
@@ -97,13 +105,24 @@ function empty(status: number, headers?: Record<string, string>): Response {
 	return new Response(null, { status, headers });
 }
 
-function isAuthorized(req: Request, tokens: ReadonlySet<string>): boolean {
-	if (tokens.size === 0) return true;
+function requestBearer(req: Request): string | undefined {
 	const header = req.headers.get("authorization");
-	if (!header) return false;
+	if (!header) return undefined;
 	const match = header.match(/^Bearer\s+(.+)$/i);
-	if (!match) return false;
-	return tokens.has(match[1].trim());
+	return match?.[1].trim() || undefined;
+}
+
+function isAuthorized(req: Request, tokens: ReadonlySet<string>, tokenHashes: readonly Buffer[]): boolean {
+	if (tokens.size === 0 && tokenHashes.length === 0) return true;
+	const candidate = requestBearer(req);
+	if (!candidate) return false;
+	if (tokens.has(candidate)) return true;
+	if (tokenHashes.length === 0) return false;
+	const digest = createHash("sha256").update(candidate).digest();
+	for (const hash of tokenHashes) {
+		if (timingSafeEqual(digest, hash)) return true;
+	}
+	return false;
 }
 
 function supportsCodexMeterBlockScopes(req: Request): boolean {
@@ -424,6 +443,7 @@ async function serveSnapshot(
 	gate: GenerationGate,
 	refresher: AuthBrokerRefresher | undefined,
 	peer: string,
+	shutdownSignal: AbortSignal,
 ): Promise<Response> {
 	await storage.reload();
 	const clientSupportsCodexMeterBlockScopes = supportsCodexMeterBlockScopes(req);
@@ -443,7 +463,7 @@ async function serveSnapshot(
 
 	const delay = delayResult(waitMs);
 	const waitController = new AbortController();
-	const waitSignal = AbortSignal.any([req.signal, waitController.signal]);
+	const waitSignal = AbortSignal.any([req.signal, waitController.signal, shutdownSignal]);
 	const result = await Promise.race([gate.waitForChange(clientGeneration, waitSignal), delay.promise]);
 	delay.cancel();
 	waitController.abort();
@@ -495,6 +515,7 @@ function serveSnapshotStream(
 	refresher: AuthBrokerRefresher | undefined,
 	peer: string,
 	keepaliveMs: number,
+	shutdownSignal: AbortSignal,
 ): Response {
 	const encoder = new TextEncoder();
 	const openedAt = Date.now();
@@ -522,6 +543,7 @@ function serveSnapshotStream(
 		}
 		if (abortHandler) {
 			req.signal.removeEventListener("abort", abortHandler);
+			shutdownSignal.removeEventListener("abort", abortHandler);
 			abortHandler = null;
 		}
 		try {
@@ -610,7 +632,12 @@ function serveSnapshotStream(
 	const stream = new ReadableStream<Uint8Array>({
 		async start(c) {
 			controller = c;
+			abortHandler = (): void => cleanup();
+			req.signal.addEventListener("abort", abortHandler);
+			shutdownSignal.addEventListener("abort", abortHandler);
+			if (req.signal.aborted || shutdownSignal.aborted) { cleanup(); return; }
 			await storage.reload();
+			if (closed) return;
 			const initial = buildSnapshot(storage, refresher, clientSupportsCodexMeterBlockScopes);
 			lastGeneration = initial.generation;
 			for (const entry of initial.credentials) lastByCredId.set(entry.id, fingerprintEntry(entry));
@@ -623,8 +650,6 @@ function serveSnapshotStream(
 			unsubscribe = storage.onGenerationChanged(() => {
 				void processGenerationBump();
 			});
-			abortHandler = (): void => cleanup();
-			req.signal.addEventListener("abort", abortHandler);
 			logger.info("auth-broker stream opened", { peer, generation: initial.generation });
 		},
 		cancel() {
@@ -648,6 +673,24 @@ function serveSnapshotStream(
 export function startAuthBroker(opts: AuthBrokerServerOptions): AuthBrokerServerHandle {
 	const bind = parseBind(opts.bind ?? DEFAULT_AUTH_BROKER_BIND);
 	const tokens = new Set<string>(opts.bearerTokens);
+	const tokenHashes: Buffer[] = [];
+	if (opts.bearerTokenHashes !== undefined) {
+		if (!Array.isArray(opts.bearerTokenHashes) || opts.bearerTokenHashes.length === 0) {
+			throw new AIError.ConfigurationError("bearerTokenHashes must be a nonempty array of SHA-256 digests.");
+		}
+		for (const hash of opts.bearerTokenHashes) {
+			if (typeof hash !== "string" || hash.length !== 64 || !/^[0-9a-f]{64}$/.test(hash)) {
+				throw new AIError.ConfigurationError("bearerTokenHashes must contain exact lowercase SHA-256 digests.");
+			}
+			tokenHashes.push(Buffer.from(hash, "hex"));
+		}
+	}
+	const controlBearerToken = opts.controlBearerToken;
+	if (controlBearerToken !== undefined && (typeof controlBearerToken !== "string"
+		|| controlBearerToken.length === 0 || controlBearerToken.trim() !== controlBearerToken
+		|| !tokens.has(controlBearerToken))) {
+		throw new AIError.ConfigurationError("controlBearerToken must be a configured nonempty plaintext bearer.");
+	}
 	const version = opts.version;
 	const streamKeepaliveMs = opts.streamKeepaliveMs ?? DEFAULT_STREAM_KEEPALIVE_MS;
 	const externalChangePollMs = opts.externalChangePollMs ?? DEFAULT_EXTERNAL_CHANGE_POLL_MS;
@@ -659,8 +702,12 @@ export function startAuthBroker(opts: AuthBrokerServerOptions): AuthBrokerServer
 				refreshSkewMs: opts.refreshSkewMs ?? DEFAULT_REFRESH_SKEW_MS,
 				refreshIntervalMs: opts.refreshIntervalMs ?? DEFAULT_REFRESH_INTERVAL_MS,
 			});
-	refresher?.start();
-	const generationGate = new GenerationGate(opts.storage, externalChangePollMs);
+	let state: "active" | "draining" | "drained" = "active";
+	let drainPromise: Promise<void> | undefined;
+	const shutdown = new AbortController();
+	let closePromise: Promise<void> | undefined;
+	let requests = 0;
+	let requestsDrained: (() => void) | undefined;
 
 	const server = Bun.serve({
 		hostname: bind.hostname,
@@ -669,22 +716,38 @@ export function startAuthBroker(opts: AuthBrokerServerOptions): AuthBrokerServer
 		fetch: async (req): Promise<Response> => {
 			const url = new URL(req.url);
 			const pathname = url.pathname;
+			if (pathname === "/v1/control/state" || pathname === "/v1/control/quiesce") {
+				if (controlBearerToken === undefined) return json(404, { error: "control unavailable" });
+				if (requestBearer(req) !== controlBearerToken) return json(401, { error: "unauthorized" });
+				if (pathname === "/v1/control/state" && req.method === "GET") return json(200, { state });
+				if (pathname === "/v1/control/quiesce" && req.method === "POST") {
+					try {
+						await quiesce();
+						return json(200, { state });
+					} catch {
+						return json(503, { error: "broker drain failed" });
+					}
+				}
+				return json(405, { error: "method not allowed" });
+			}
+			if (state !== "active") return json(503, { error: "broker is draining" });
 			const peer =
 				req.headers.get("x-forwarded-for")?.split(",")[0].trim() || req.headers.get("x-real-ip") || "unknown";
+			requests++;
 			try {
 				if (req.method === "GET" && pathname === "/v1/healthz") {
 					const body: HealthzResponse = { ok: true, version };
 					return json(200, body);
 				}
-				if (!isAuthorized(req, tokens)) {
+				if (!isAuthorized(req, tokens, tokenHashes)) {
 					logger.info("auth-broker request unauthorized", { method: req.method, path: pathname, peer });
 					return json(401, { error: "unauthorized" });
 				}
 				if (req.method === "GET" && pathname === "/v1/snapshot/stream") {
-					return serveSnapshotStream(req, opts.storage, refresher, peer, streamKeepaliveMs);
+					return serveSnapshotStream(req, opts.storage, refresher, peer, streamKeepaliveMs, shutdown.signal);
 				}
 				if (req.method === "GET" && pathname === "/v1/snapshot") {
-					return serveSnapshot(req, url, opts.storage, generationGate, refresher, peer);
+					return await serveSnapshot(req, url, opts.storage, generationGate, refresher, peer, shutdown.signal);
 				}
 				if (req.method === "GET" && pathname === "/v1/usage") {
 					try {
@@ -765,7 +828,9 @@ export function startAuthBroker(opts: AuthBrokerServerOptions): AuthBrokerServer
 				if (refreshMatch) {
 					const id = Number.parseInt(refreshMatch[1], 10);
 					try {
-						const entry = await opts.storage.refreshCredentialById(id, req.signal);
+						// The broker owns this mutation, not the HTTP connection: disconnecting
+						// must not let shutdown race a provider's already-rotated credential.
+						const entry = await opts.storage.refreshCredentialById(id);
 						const body: CredentialRefreshResponse = { entry };
 						logger.info("auth-broker credential refreshed", {
 							id,
@@ -884,9 +949,33 @@ export function startAuthBroker(opts: AuthBrokerServerOptions): AuthBrokerServer
 					error: String(error),
 				});
 				return json(500, { error: "internal error" });
+			} finally {
+				if (--requests === 0) requestsDrained?.();
 			}
 		},
 	});
+	// A failed fixed-port bind must not start another canonical refresher.
+	refresher?.start();
+	const generationGate = new GenerationGate(opts.storage, externalChangePollMs);
+	const quiesce = (): Promise<void> => {
+		if (drainPromise) return drainPromise;
+		state = "draining";
+		shutdown.abort();
+		const requestsStopped = requests === 0 ? undefined : new Promise<void>(resolve => { requestsDrained = resolve; });
+		drainPromise = (async () => {
+			const results = await Promise.allSettled([
+				refresher?.stop(),
+				requestsStopped,
+				Promise.resolve().then(() => generationGate.close()),
+			]);
+			// A usage timeout may have completed the HTTP handler while its
+			// single-flight OAuth rotation still owns the durable credential.
+			await opts.storage.drainRefreshes();
+			for (const result of results) if (result.status === "rejected") throw result.reason;
+			state = "drained";
+		})();
+		return drainPromise;
+	};
 
 	const boundHost = server.hostname ?? bind.hostname;
 	const boundPort = server.port ?? bind.port;
@@ -894,10 +983,19 @@ export function startAuthBroker(opts: AuthBrokerServerOptions): AuthBrokerServer
 		url: `http://${boundHost}:${boundPort}`,
 		port: boundPort,
 		hostname: boundHost,
-		close: async () => {
-			refresher?.stop();
-			generationGate.close();
-			server.stop(true);
+		quiesce,
+		close: () => {
+			if (closePromise) return closePromise;
+			closePromise = (async () => {
+				try {
+					await quiesce();
+				} finally {
+					// Explicit close retires the listener only after every owned
+					// operation has settled, including a failed drain.
+					await server.stop(true);
+				}
+			})();
+			return closePromise;
 		},
 	};
 }
diff --git a/src/auth-storage.ts b/src/auth-storage.ts
index 2ac6e6bb314c95178a04b4d519ba34282c94230b..67abd190e9490c554b392c5bdd354d99245f28d8 100644
--- a/src/auth-storage.ts
+++ b/src/auth-storage.ts
@@ -1471,6 +1471,30 @@ export class AuthStorage {
 		this.#store.close();
 	}
 
+	/**
+	 * Join owned refresh work after the caller has stopped request/sweep admission.
+	 * Usage cancellation can detach a credential refresh, and serialized usage
+	 * probes can create later refreshes. Join both roots and the actual refresh
+	 * maps to a fixed point; do not replace this with another refresh request.
+	 */
+	async drainRefreshes(): Promise<void> {
+		let failure: PromiseRejectedResult | undefined;
+		for (;;) {
+			const pending = [
+				...this.#usageReportsInFlight.values(),
+				...this.#usageRequestInFlight.values(),
+				...this.#oauthRefreshInFlight.values(),
+				...this.#oauthCredentialRefreshInFlight.values(),
+			];
+			if (pending.length === 0) break;
+			const results = await Promise.allSettled(pending);
+			for (const result of results) {
+				if (result.status === "rejected") failure ??= result;
+			}
+		}
+		if (failure) throw failure.reason;
+	}
+
 	getGeneration(): number {
 		return this.#generation;
 	}
